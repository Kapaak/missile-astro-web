---
interface Props {
  class?: HTMLHtmlElement["className"];
  name?: string;
  id?: string;
  cols?: string;
  rows?: string;
}

const { class: className = "", ...rest } = Astro.props;
---

<textarea class={`py-3 px-2 bg-tetriary text-grey-200 ${className}`} {...rest}
></textarea>
