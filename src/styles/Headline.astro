---
interface Props {
	className?: HTMLHtmlElement["className"];
}

const { className = "" } = Astro.props;
---

<h1
	class={`text-grey-200 uppercase font-normal font-secondary text-4xl ${className}`}
>
	<slot />
</h1>
